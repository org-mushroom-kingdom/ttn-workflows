name: Caller Permission Test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test-job:
    name: "Test job for caller permissions"
    runs-on: ubuntu-latest
    permissions:
        contents: read
    uses: ./.github/workflows/reusable-workflow-test.yml